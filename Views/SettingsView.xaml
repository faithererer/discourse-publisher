<hc:Window x:Class="DiscoursePublisher.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="设置" Width="500" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="论坛地址 (URL):" VerticalAlignment="Center"/>
        <hc:TextBox Grid.Row="0" Grid.Column="1" Text="{Binding DiscourseUrl}" Margin="10,0,0,0"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="API Key:" VerticalAlignment="Center" Margin="0,10,0,0"/>
        <hc:TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ApiKey}" Margin="10,10,0,0"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="API 用户名:" VerticalAlignment="Center" Margin="0,10,0,0"/>
        <hc:TextBox Grid.Row="2" Grid.Column="1" Text="{Binding ApiUsername}" Margin="10,10,0,0"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource ButtonPrimary}" Content="保存" Command="{Binding SaveCommand}" IsDefault="True"/>
            <Button Content="取消" Margin="10,0,0,0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</hc:Window>